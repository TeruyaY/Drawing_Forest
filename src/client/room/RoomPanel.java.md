# RoomPanel.java

担当A（ルーム管理）のロビー画面UI（`JPanel`）。ユーザー名・部屋名の入力欄と「作成」「更新」ボタン、部屋一覧のリスト表示（ダブルクリックまたは「参加」ボタンで入室）、現在の部屋名と待機中メンバーの一覧表示、「ゲーム開始」ボタン、ステータスメッセージ表示欄を備える。ボタン操作は `RoomController` の各メソッドを呼び出し、サーバーからの更新は `setRooms()` / `setMembers()` / `setCurrentRoom()` / `showStatus()` を通じて画面に反映される。
