# .gitignore

Gitの管理対象から除外するファイル・ディレクトリを指定する設定ファイル。コンパイル済みクラスファイル（`*.class`, `bin/`, `out/`, `build/`）、IDEやエディタの設定ファイル（`.idea/`, `.vscode/`, `.classpath` など）、OSが生成する一時ファイル（`.DS_Store`, `Thumbs.db`）を対象としている。
