# DrawLauncher.java

担当B（お絵描き）機能を単体で動作確認するためのランチャー。コマンドライン引数（サーバーIP・ポート・部屋ID、省略時は localhost:8080/testroom）を受け取ってサーバーへ接続し、色選択ツールバー付きの描画ウィンドウを表示する。同じ部屋IDで2つ以上起動すると、片方で描いた線がもう片方にリアルタイムで反映されることを確認できる。最終的にはA/D担当のUIから `DrawController` を組み込む想定の検証用エントリポイント。
